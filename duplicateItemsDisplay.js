const DuplicateDisplay =(array) =>{
    array.filter((item,index)=>{
        index !== array.indexOf(item)});
    return array;}

// finding number of occurrance in an array
array.filter(x => x === element).length
